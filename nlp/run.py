from module import app, config

# if config['APP_STAGE'] == 'DEVELOPMENT':
#     app.run(host=config['APP_HOST'], port=config['APP_PORT'], debug=True, threaded=True)
# elif config['APP_STAGE'] == 'PRODUCTION':
#     app.run(host='0.0.0.0', port=config['APP_PORT'], debug=True, threaded=True)

